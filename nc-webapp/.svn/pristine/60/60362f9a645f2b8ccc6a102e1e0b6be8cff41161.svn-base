package au.com.toyota.nc.webapp.stripes.staff.search;

public interface DealerStaffSearchValidator
{
    boolean isValid();

    /**
     * The key to the validation error message in a properties file. This will be looked up if the isValid() call
     * fails, as a validation message will then need to be displayed
     */
    String getValidationErrorMessageKey();
}
